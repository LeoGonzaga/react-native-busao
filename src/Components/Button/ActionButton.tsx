import React from 'react';
import {View} from 'react-native';

import {Container, TitleButton} from './style';

const Button: React.FC = () => {
  return (
    <Container>
      <TitleButton>Entrar</TitleButton>
    </Container>
  );
};

export default Button;
